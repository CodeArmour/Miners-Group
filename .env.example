# Contact destination used by mailto-based forms until a backend is connected.
NEXT_PUBLIC_CONTACT_EMAIL=hello@minersgroup.com

# Optional analytics preparation. Leave empty until a provider is chosen.
# Supported script-style providers can be wired by setting these in deployment.
NEXT_PUBLIC_ANALYTICS_PROVIDER=
NEXT_PUBLIC_ANALYTICS_SRC=
